# PS Source Status

## Current status

`ps.md` contains the exact source text that could be reliably transcribed from the supplied problem-statement image.

The image ended at:

> “capable of creating unique spatial identities for:”

The conversation referenced another image containing additional content, but its bytes were not available to the build environment for reliable transcription.

## Required next action

Upload/re-attach the complete PS screenshot or the official PS PDF/text.

When received:

1. Verify the entire statement.
2. Do NOT edit `ps.md`.
3. Create `ps-2.md` containing the complete verified PS.
4. Record the event in `progress.md`.
5. Update `requirements-traceability.md`.
6. Do not silently change `solution.md`; create `solution-2.md` if the full PS changes the proposed solution.
